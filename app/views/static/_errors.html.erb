<% if model.errors.any? %>
  <div>
    <h2><%= pluralize(model.errors.count, "error") %></h2>

    <ul>
      <% model.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>